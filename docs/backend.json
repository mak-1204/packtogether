{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents additional information about an authenticated user, complementing an external authentication system. Contains non-sensitive user profile data.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity. This typically matches the user's ID from an external authentication system."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user, used for personalized greetings."
        },
        "mobile": {
          "type": "string",
          "description": "The user's registered mobile number."
        }
      },
      "required": [
        "id",
        "firstName"
      ]
    },
    "Trip": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Trip",
      "type": "object",
      "description": "Represents a single trip plan, encompassing its core details, budget, and organizational information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Trip entity."
        },
        "name": {
          "type": "string",
          "description": "The name given to the trip by the organizer."
        },
        "destination": {
          "type": "string",
          "description": "The primary destination for the trip."
        },
        "startDate": {
          "type": "string",
          "description": "The planned start date of the trip.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The planned end date of the trip.",
          "format": "date-time"
        },
        "budgetPerHead": {
          "type": "number",
          "description": "The estimated budget per person for the entire trip."
        },
        "vibe": {
          "type": "string",
          "description": "The overall style or nature of the trip (e.g., Budget, Mid-Range, Luxury).",
          "format": "enum"
        },
        "organizerId": {
          "type": "string",
          "description": "Reference to the UserProfile who created and organizes this trip."
        },
        "status": {
          "type": "string",
          "description": "The current planning status of the trip (e.g., Planning, Ongoing, Completed).",
          "format": "enum"
        },
        "coverImageUrl": {
          "type": "string",
          "description": "URL for a cover image representing the trip's destination."
        },
        "totalPlannedBudget": {
          "type": "number",
          "description": "The sum of planned budgets from all itinerary items for the trip."
        },
        "totalActualBudget": {
          "type": "number",
          "description": "The sum of actual expenses from all itinerary items for the trip."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the trip record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the trip record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "destination",
        "startDate",
        "endDate",
        "budgetPerHead",
        "vibe",
        "organizerId",
        "status",
        "coverImageUrl",
        "totalPlannedBudget",
        "totalActualBudget",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores individual user profile data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the authenticated user."
            }
          ]
        }
      },
      {
        "path": "/users_by_mobile/{mobile}",
        "definition": {
          "entityName": "MobileLookup",
          "schema": {
            "type": "object",
            "properties": {
              "uid": { "type": "string" }
            }
          },
          "description": "Used to check if a mobile number is already registered.",
          "params": [
            {
              "name": "mobile",
              "description": "The user's mobile number."
            }
          ]
        }
      }
    ]
  }
}
